# --- core ---
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
SQLAlchemy>=2.0
PyYAML>=6.0
python-dateutil>=2.9
tenacity>=8.4            # retry/backoff for connectors
structlog>=24.1         # structured JSON logs

# --- drafting / classification ---
anthropic>=0.34         # Claude API (drafting brain)
httpx>=0.27             # Ollama HTTP + general
# ollama runs as a separate local service; we call it over HTTP

# --- retrieval ---
lancedb>=0.9            # embedded vector store
# embeddings: sentence-transformers OR Ollama embeddings (see retrieval/embeddings.py)
sentence-transformers>=3.0

# --- collectors ---
google-api-python-client>=2.130   # Gmail + Calendar
google-auth-oauthlib>=1.2
google-auth>=2.30
telethon>=1.36                    # Telegram user client
playwright>=1.45                  # Skool / (later) WhatsApp, TikTok
beautifulsoup4>=4.12              # HTML sanitization helper
lxml>=5.2

# --- macOS integration ---
keyring>=25.2           # macOS Keychain backend
# pyobjc-framework-* optional for native notifications; we shell out to osascript

# --- dev / test ---
pytest>=8.2
